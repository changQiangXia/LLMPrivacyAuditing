# Data Layout

- `raw/`: source JSONL files such as `train.jsonl` and `nonmember.jsonl`
- `processed/`: generated files such as canary-injected training data and canary metadata

This cleaned copy intentionally does not include local datasets.
